#include "stdafx.h"


int main(void)
{int A,B,C;
 scanf("%d%d%d", &A, &B,&C);
 if(A<B && B<C)
 {printf("\n %d %d %d",A,B,C);}
 else
 {  if(B<A && A<C)
    {printf("\n%d %d %d ", B,A,C);}
    else
    { if(C<A && A<B)
      {printf("\n%d %d %d ", C,A,B);
      }
	  else
	  {  if(C<B && B<A )
         {printf("\n%d %d %d ", C, B,A);
         }
	     else
		 {  if(A<C && C<B)
            {printf("\n%d %d %d ", A,C,B); 
            }
		    else
			{printf("\n%d %d %d ", B,C,A);
            }
         }
      }
    }
 
 }
	return 0;
}

